const assert = require('assert');
const getPhase = require('../js/get-phase.js');

describe('#getPhase()', function() {

  xit('should be a function', function() {
    assert.deepEqual(typeof getPhase, 'function');
  })

  xit('should return "progress" when the game is in progress', function() {
    
  })

  xit('should return "x_win" if "x" player wins', function() {
    
  })

  xit('should return "o_win" if "o" player wins', function() {
    
  })

  xit('should return "draw" in a case of draw', function() {
    
  })

  

})